LONG TERM FEATURES
    - Refresh price button on research and index that shows when last price was fetched.
    - Add seed transactions to create various mock users of different investment profiles

I will require the 'portfolio' data for the logged in user in at least 3 places in this app, but it's the most computationally expensive route in the api.

This is because it needs to process a user's entire transaction history and then process over it in multiple steps to handle things like 
    -group by company
    -drop closed positions
    -stock splits
    -cost basis. 
Any other route that is querying from a large set of datapoints is doing so in the sql engine on an indexed table so this portfolio calculation is unique. 

7/9/26
I initially considered making a different route that would serve my 'top/bottom' movers widget, but that seems like overkill and instead of doing 75+% of the computation and serving a subset of the portfolio data on a /top_bottom_movers route i'm going to just cache the calculaton instead.

In python, once a module is imported it lives in memory for the lifetime of the process and is accessed via sys.modules.
Anything in the module scope or the class var scope is accessible through this globally within a given process.

This means I can use a class var to hold the 'user_holdings' state once computed and expose class methods to access and mutate it where needed throughout the app. logout(delete) transact(recalculate) fetch_holdings(read). 

This is "event-based cache invalidation". and will not be refreshed on a timer. Although, im realizing now that I cant just cache the finished user portfolio data because it will be invalidated by every price lookup. Instead i'll draw the cache boundry at stock_id: [qty, cost_basis, total_cost] and have the server re-compute the columns derived from current price live every time, but this will be much less expensive. 

An alternative is to expand my "balance_snapshots" system to hold the qty owned data required to calculate top and bottom movers. 

Either way, I need to nail down exactly what work balance snapshots and user holdings are doing to decide what to cache, when to update it, and who can read it.

7/10/26
THE PLAN!
    Goal: Get the 'screeners' to each render in their own frames. that goes card(scrollable table)
    
    Currently the screeners are rendering correctly. IF i remember correctly there's not much I 
can do to effect the screeners im getting from the api. This feels like a good opportunity
to make this backend better by just allowing a direct throughline from the screeners data source
and the api. Currently i arbitrarily cut off the data flow and pick a subset. And make my own
For now though, assuming im working with the fixed set of screeners.
I'm thinking I should examine the way the screener data is currently captured and investigate that.
    Q: What is the source of the data?
    A: it appears the "YahooQueryService" method "yq_screener_fetch_screeners()" is the source.
        This method makes an instance of the YahooQuery class "ticker"
        It then requests a list of screeners.
        Q: Can I improve this?
        A: Yes, expose the entire list of screeners at once from yq.
            screener.available_screeners
            Note: Be sure to time different qty's of data from the fetch

    Q: What is the shape that the database expects.
    Q: What is the path it takes to get there?
    A:
        1. Fetch Screeners
            - Call YQ.Ticker for screeners.
        2. Filter Screener data
            - Filters out screeners 
            under 50m market cap
            share price under a dollar
            pink slips and over the counter
            average daily volume below 200,000
            Does not appear to change the shape of the data.
        3. Extracts a custom "relative volume" screener.
            Q: From where?
            A: Filtered screeners, which is a list of companies which showed up in
            any of the screeners. The screeners come from the yq source as A
            dict with "screener name" as the key, and a dict of data about a company
            This dict isn't specific to the screener just associated via this 
            dict.
                Note: This means that exposing the entire dataset of screeners
                will also massively improve these 'custom' screeners who just take
                a subset of the total screeners available since we're only looking
                at a small portion of all companies that exist.
        4. Extracts screener "metadata"
            Q: What is that?
            A: This is simply the list of company tickers in each screener. 
            Q: What is it used for?
            A: In the "set_screeners_metadata" method of the APIDataIO() class.
                All metadata is deleted
                And replaced with the fresh set.
                Retrieving constructed 'quote' objects that are populated with data
                in the db. 
                Note: This is a big missed opportunity to just update the database.
                In my future yq_cache I would want to just keep this quote object
                as the central shape that a ticker is in. Currently it's pointlessly 
                complex and disparate. For now i need to just work with the current subset.
        5. Extracts a subset of the data that is relevant to the app, including constructing
        a 'price' module so the screener can be introduced into the 'tickers' table.
            Note: this has the effect of allowing users to see it in search bar and other stuff
            i cant be bothered to look into right now.
        6. Upserts all tickers from the screeners found into symbols table.
        7. Writes everything to the db.

        This pipeline is known as screener_data_update_orchestrator and is called at the
        start of each call to the /screeners route.

        Okay, at this point i've come to the conclusion that I could improve this pipeline 
        but in order to achieve the current goal of just exposing all the screeners instead
        of a tiny subset, all I need to do is 

        Q: Okay, its up to date, but how is the data read?
        A: get_screener_results()
            Selects the screeners whose names we want to read.
            WAIT! This is pointless, if anything I want the control
            system to be frontend asks for a subset of screeners as a 
            query parameter but there's no reason for this mechanism if I'm
            going to expose all yqs data. This is also the same list of screeners
            the write side is referencing so at least that's good. That means I could
            also just add every screener name to the codebase and that would allow me to remove some
            instead of just saying get all. 


    After Class:
        Read about the 'factory pattern' and then review the utility of this implementation on
        lines 29-31 in the YahooQueryService.py module.
